# Mobile (and web prod) build talks to the deployed API. Render also injects
# VITE_API_BASE for the web static-site build; both resolve to the same URL.
VITE_API_BASE=https://worldcup-api-y9t7.onrender.com/api
